# Config of the system being used for training
system_config = {
    # Processor cores to use, e.g. for preprocessing of datasets
    "threadpool_size": 24,
    # Training batch size; depends on GPUs VRAM
    "training_batch_size": 100
}